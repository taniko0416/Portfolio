### this script is only used for execution of rails new.
# docker-compose run --rm webapp bundle exec rails new . --force --no-deps \
#   --database=mysql \
#   --skip-keeps \
#   --skip-sprockets \
#   --skip-turbolinks \
#   --skip-webpack-install

# cd webapp
# rm -rf README.md .git
